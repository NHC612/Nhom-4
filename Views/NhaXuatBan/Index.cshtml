@model IEnumerable<Super_Book_Store.Models.NhaXuatBan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.NXBName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NXBName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NXBName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NXBName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NXBName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
